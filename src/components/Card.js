import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Card extends Component {
  render() {
    return (
      <div>
        Card
      </div>
    );
  }
}

Card.propTypes = {

};

export default Card;
